1001 
求a[i]+a[j]==K的对数
solution:
排序O(nlogn)后双指针O(n)
--------------
1283
给出举行面积，求最小周长
solution:
分解因子
--------------
1080
求x^2+y^2==N的对数
solution:
双指针,注意可以为等于的情况
即i<=j
-------------
1284
求1~N中有多少个数不是2,3,5,7的倍数
solution:
容斥
----------------
1087
询问某个数是否存在.
solution:
等于val的个数:
upper_bound(x,x+N,val)-lower_bound(x,x+N,val)
------------------
1082
询问与7无关的数
solution:
预处理出前缀和
-------------------
1083
矩阵取数问题
solution:
dp[i][j]:走到i,j能获得的最大价值
------------------
1003 
询问N!后有多少个0(N<=1e6)
solution 1:
求1~N中,每个数唯一分解后有多少个2,5.
自大到小容斥即可,即2^k,2^(k-1),2^(k-2)
答案是:min(cnt2,cnt5)
solution 2:
	while(N){s+=N/5;N/=5;}
---------------------

1090 
询问一个数组中和0的三元组的个数，数组里面任意两个数不相等(N<=1000).
solution 1:
O(N^2)
先折半，然后问题关键在于去重,每一个node存储两个值，一个是合成后的val
还有一个是id,二分查找到和为0的范围后，判断id是否重复即可。
二分代码:
for(int i=0;i<N;++i){
	node t;t.id=-1;
	t.val=-a[i];
	int begin=lower_bound(no,no+top,t)-no;
	t.val=-a[i]+1;
	int end=lower_bound(no,no+top,t)-no;
	for(int j=begin;j<end;++j){//do something}
}
solution 2:
枚举起点i,然后尺取法O(N)求j,k
----------------------
1381
抛半径为R硬币半径与直线相交的条数.直线间距为1.
solution:
ans=2*R

---------------------------
1091 贪心*
给你N段线段，请你从中选出两条线段，使得他们的重叠部分最大。
solution:
先sort,按起点从小到大排序，起点相同，终点从大到小排序
维护一个max_end,既右端点最多延伸到哪了
ans=max(ans,min(max_end,no[i].r)-no[i].l);
------------------------
1004
求N^N的最后一位数
solution:
快速幂或者找规律
-------------------------
1002
数塔取数问题
solution:
基础dp

---------------------------
1305
给你N个数,求任意两个数a,b (a+b)/(a*b)
有点意思
solution:
某个为1: ans=1
两个1: ans=2
两个2: ans=1
其他情况为0
求和即可
----------------------------
1344
询问初始值至少为多少，才能保证序列的所有前缀和都>=0
solution:
直接扫描一遍，输出前缀和的最小值的相反数

-----------------------------
1289 *有趣的题目
给出所有鱼的方向以及大小,大鱼吃小鱼,问最后有多少条鱼
solution:
首先观察最后答案的形式，一定是 <- <- <- ... -> -> ->
所以我们把两个方向分离，正向做一次，反向做一次
每个方向遍历一遍时只考虑另外一个方向的会不会被吃掉
code:
int prev=-1;
for(int i=1;i<=N;++i){
	if(d[i]==1){
		prev=max(prev,x[i]);
	}else{
		if(prev>x[i])visit[i]=1;
		else prev=-1;
	}
}
---------------------------------
2517 01数组变成不下降数组的代价
solution:
直接枚举临界点,O(1)计算,复杂度O(N)

--------------------------------
2423 有根树叶节点数量
solution:
找到度数为0的点．特判根节点度数是否为1

---------------------------
2489 n轮操作，每次将i的倍数的灯泡状态取反，问有多少亮着
solution:
考虑一个数的因子个数，就是被操作的次数．判断因子个数奇偶性
key:只有完全平方数的因子个数是奇数

----------------------------
2488 求两个矩形面积并的大小 好题
solution:
先求两个矩形面积交z
solution:x+y-z
求矩形面积交，利用投影，计算出重合长度
    long long x_1=myabs(c-a)+myabs(g-e);
    long long x_2=max(c,g)-min(a,e);  

-----------------------------
2656 阿克曼函数
solution:
记忆化搜索即可，比较坑.
m<=3,n<=16,但数组如果只开dp[4][17]会出问题，中间会超过这个值.

------------------------------
2651 区间和的和
一共有N*(N+1)/2个区间，求区间和的和.
solution:
画一下，找规律,复杂度O(N)
a[i]这个数字被算i*(n-i+1)次?why
考虑到a[i]被区间的覆盖次数即可

---------------------------------
2529 移动 **好题**
x轴上有n个点，每次可以移动到1-n人任一个点.不能不动.
可以移动k次,问是否存在一种移动方法,恰好使移动步数为S.
solution:
可以移动的步数范围 k<=x<=k*(n-1)
极大值极小值可以理解，为什么中间连续的?
因为比如 9 8 7 6 9这种答案，可以重新排序9 9 8 7 6把范围减小
所以最后答案一定存在


