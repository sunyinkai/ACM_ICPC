1621 花钱买车牌
1632 B君的连通，考虑到如果炸k条边，一定是(k+1)个连通块，与树的形状无关，用组合恒等式即可。
------------
1043 幸运号码 给一个N<=1000,数字长度为2*N，求有多少个"前N个数等于后N个数"
dp[i][j]:当前处理到第i位和为j的方案数,前N个不能有前导0,后N个可以有前导0
滚动数组优化

---------------
1096 x轴上N个点，找到一点到其他所有点的距离和最小
直接输出中点即可，why? 
python绝对值函数:abs

------------------
1068
两个人轮流拿石子,只能为2^n,拿到最后一棵的胜利
solution:
判断x%3==0即可,利用NP态的性质

------------------
1127
求最小的子串包含所有的A-Z字符 len<=1e5
solution:
经典题目
考虑到单调性,尺取法即可

------------------
1268
给你20个数,问能否取一些求和为K
solution:
枚举1~(1<<N)-1,二进制下0代表不取,1代表取.检验即可

---------------------
1640
求一棵生成树让最大边最小,满足这个的条件下,生成树之和要尽可能大
solution:
- 二分最大的边,
- 跑最大生成树

--------------------
1770
求a{n} * b 的结果中有多少个d
solution:
易错题目
只考虑最首一位和最末两位即可,中间都是重复的

---------------------
1163
活动安排活动,
每个活动有个截止时间e[i],执行时间为1,做完活动后得到价值w[i]
问最大的价值
solution:
1,优先队列
从后往前扫描,将遇到的价值加入优先队列,然后计算两个活动间的gap,即可

----------------------
2,并查集优化
先考虑暴力写法,再用并查集优化

---------------------
1246


-----------------------
1247
问能不能到从(a,b)到(x,y)
(a,b) -> (a+b,b),(a,a+b),(a-b,b),(a,a-b)
solution:
判断两个的gcd是否相等.

-----------------------
1276 *好题 
多次询问一个数字Q,询问大于数字Q的数字能够形成多少个联通块
询问次数N,数字个数Q<=5e4
solution:
自底向上一个一个减,不好做,我们就可以自顶向下加
考虑到每次加入一个数字时,只会与它前面或者后面的相关.
我们可以考虑自顶向下分析,每次新加入一个岛屿ans++,然后看其是否与前面,后面相邻,ans--,然后每一段结束后,记录答案.

----------------------------
1270 *好题
给你N个数，a[i],让你构建一个数组b[i],使得1<=b[i]<=a[i]
让sgima(a[i]-a[i-1])的和最大

solution:
贪心的想法，如果其它的都确定了，我们选择极大或者极小可以获得更多
可以推导出:
考虑每个数要么取极小，要么取极大
O(N) dp即可 dp[i][0]:当前处理到i,0代表前面为极小，1代表为极大.

思考:
最后答案一定是1,x,1,y...或者x,1,y,1的形式吗,错(X)
test case:2 9 2 2 9 2

--------------------------
1285
把N个点分为K段，要求每段含的点数相等，并且含有山峰:
p>0>N-1 且 a[p-1]<a[p]<a[p+1]

solution:
直接分解因子然后判断O(N)*cnt(cnt为因子个数)
有一个坑点:只要保证山峰的点在段内就行，并不需要左右护法都在

-------------------------------
1393  *O(N)，思维
给定一个串，求最长的子串1和0的个数相等
solution:
把'1'当做1,'0'当做-1,既求最长的子串和0,既两个的前缀和相等。
用map or unordered_map 记录一下即可

-------------------------------
1117   哈夫曼树
将一根棍子切为指定长的N段，将一根棍子分为a,b的代价为a+b
solution:
我们考虑逆过程，将木棍合在一起。
哈夫曼树，优先队列,O(NlogN)

---------------------------------
1272  * 好题
求所有顺序对之间的最大距离,即a_i<=a_j(i<=j),求max{j-i}
solution 1: 线段树
离散化以后，线段树区间的最小下标
即,遍历一遍，枚举终点，表示以a[i]结尾的数，在所有小于a[i]的数中能找到的最小下标。 O(NlogN)

solution 2:  排序+扫描
node{int id,int val}
按val从小到大排序后，
问题转化为既求i<j,a[j]-a[i]的最大值,从后往前扫一遍.
O(N)求i<j,max(a[j]-a[i])

solution 3: 二分+RMQ
枚举当前端点l,二分右端点r,query[l,r]中的

solution 4: 单调栈

-------------------------------------
1414
在圆上给你N个点的正多边形，每个点上有一个权值，然后要求移除一些点后，
仍然构成一个正多边形，且权值和最大。
solution:
分解质因子，然后枚举即可
---------------------------------------
1639
N个鞋带混在一起，可以想象N次后，不在有单独的鞋带,问最后只有一个环的概率
solution:
首先考虑把N条鞋带变成N-1条的概率
(2*N-2)/(2*N-1)
一直这样递归考虑下去即可

----------------------------------------
1483 有技巧的枚举 好题*
给你N个<=1e5的数，然后可以对每个数进行两种操作
乘以2或者除以2,问至少需要多少个操作，让所有数的一致。
solution:
枚举即可，把当前x能到达所有的数都表示出来

-----------------------------------------
1548 漂亮的枚举 好题*
大小为C的背包，有两种物品，重量分别为W_a,W_b,价值分别为H_a,H_b
为最多能获得多少价值
solution 1:
直接枚举每种物品取0~sqrt(C)+1个,计算出最大值即可。
为什么?
1,首先如果有某种物品重量超过了sqrt(C)
	结论显然成立
2,现在考虑两种物品的重量均小于sqrt(C)
	考虑到如果重量为W_a*W_b的空间,那么一定会是全选a或者b,
	b的数量一定小于W_a
		如果b的数量大于W_a,那么所占空间>W_a*W_b,
		我们可以通过将占这么大的空间b换取等空间的a更理智。
	故b<=W_a<=sqrt(C)证毕
-----------------------------------------
1116
给你一个字符串，已知它是K进制下K-1的倍数，问这个最小的K(2<=K<=31)
solution 1:
暴力O(36*N)  原理:(A*10+B)%base=(A*10%base+B%base)%base
solution 2:
考虑到sum之和必须是K的倍数,即可O(N)求

-------------------------------------------
1267 经典题目 4个数和为0
给你一个数组，问其中是否存在4元组和为0
solution:
折半后排序双指针，即可.O(N^2logN)
但要注意检验为0的那部分的判断部分的代码 

-------------------------------------------
1102 经典题 直方图中最大矩形
solution:
单调栈左右扫一遍,再计算
注意特殊的处理，首尾添上-1,这样才能保证出栈出完


-------------------------------------------
1098
给你M个数，从中找出N个数，让它们的均差最大
solution:
注意到，这N个数一定是连续的
排序后,枚举起点即可
O(NlogN+N)

--------------------------------------------
1099 贪心 好题*
有N个任务，执行占用A空间，执行完后存储会占用B空间(A>B),
问执行所有任务至少需要多少空间.
soluiton:
max(a,b+ohter.a)<max(other.a,a+other.b)的优先
why?
假设已经有了x,考虑两个的顺序
1 a,b
2 c,d
如果1优先的话
那么max(x+a,x+b+c)<max(x+c,x+d+a)
重载运算符即可,先选1不会变差.

--------------------------------------------
1101 完全背包 经典题
给你K种硬币，问把价值为N的换为这K种，有多少种方案
solution:
完全背包
	for(int i=1;i<=N;++i)
		for(int j=a[i];j<=V;++j)
			dp[j]+=dp[j-a[i]]

----------------------------------------------
1013 
求3^0+3^1+...+3^N
solution:
快速幂+逆元

---------------------------------------------
1035
求1/N的循环节
solution 1:
	即: 10^k%N==1,求最小的k
	why?相当于10^k%N后，它的余数和最开始的相同了，那么说明一个新的循环开始了
solution 2:
	我们不妨从余数入手，因为小数部分整除的结果是由上一次运算的余数决定的，如果某一次运算产生的余数跟前面产生的余数相同，则说明循环开始。

-----------------------------------------------
1534
移动棋子游戏,A(x,y)->(x-1,y),(x,y-1)
		 B(x,y) ->(x-1,y),(x,y-1),(x-1,y-1)
不能将棋子移到别人占了的地方．
solution:
	 如果A能挡住B,那么以后肯定能处处挡住B
	 或者A离原点近，A就胜利,B肯定挡不住
	 其他情况，后手胜

----------------------------------------------
1562 好题 思维+数据结构

给你一块大玻璃，每次横着切或者竖着切，在x的位置．
问每切割一次后的最大玻璃的面积．
询问方式:
H 2
V 2
V 3
V 1
solution:
	开两个优先队列，一个存储x方向上的，一个存储y方向上的，
	再分别开两个set维护要切割的位置，每次从优先队列取出来后然后取集合中找
	如果有，把玻璃切开再放回优先队列．不断重复，直到拿到一块不用再切的．

------------------------------------------------

1625 好题　思维+暴力
给你一个N*M的矩阵,N<=10,M<=100,你最多可以有k次机会对一排或一列染色，让每个
元素的值都为x,为最后矩阵和的最大值.

solution:
考虑到横着很少，完全可以暴力。这样矩阵横着的就固定了，
竖着的因为互不干扰直接贪心即可

-----------------------
2385 神棍题
给你一个半径为R的圆，找一个内接17边形，让它面积最大.
正k边形?


--------------
2504 双指针
判断一个序列是不是另外一个的子序列.
solution:
双指针,O(N)

-----------------
2519 贪心
给你一个数组，将其变为两两不同的数组的最小代价。
solution:
考虑到将a,b变为x,y和y,x的代价是一样的，我们可以直接将不变的保留。其它的改变为最近的即可。

-------------------
2496  O(N)枚举,滑动窗口的思想 
给你两个字符串，问能否将一个字符串重排列得到另一个字符串的子串.
solution:
记录一下s1串的各个字符的数量，然后滑动窗口移动


--------------------------
2476  思维,前缀和,瞎搞
求max{min(a_i,a_j)*(j-i)}
solution:
因为是取两个数的最小值，我们直接最小的值入手,按值排序，然后id作为val
id : 1 4 8 6 5 3 9 2 7
val: 1 2 3 4 5 6 7 8 8
扫描一遍,找到后面id的最小值和最大值(这个可以用前缀和预处理出来),计算一下即可
思考:
如果是 max{min(a_i...a_j)*(j-i)}
经典的直方图面积题目，单调栈解决。

--------------------------
2493 二进制,算贡献
问任意两个数的二进制下不同数位的个数的和。
solution:
计数每一个二进制位的个数，然后做一个乘法。

-----------------------------
2482 数学,思维,解方程
solution:
直接枚举长度
本质上:1+x+x^2+...x^n=k
n<=2 直接解方程
n>=3直接枚举

---------------------------------
2487 dp,思维
从环上选一些点，让他们不相邻，但和最大。
solution:
考虑到题目是环，有首尾限制，会影响答案。
而一条链的话却很简单。
分类讨论,如果选择a[1],那么a[3]...a[N-1]相当于一条链
如果不选择a[1],那么a[2]...a[N]相当于一条链

----------------------------
2204 贪心
连接N个字符串，让它们的字典序最小
solution:
只考虑两个，交换两个位置，不会更差，一直交换即可

---------------------------
2541 前缀和+思维

给你一个长度为N的数组,问有多少个数满足去掉这个数后剩余数组的奇数项的和等于偶数项的和.
solution:
先计算前缀和,然后考虑到去除一个数a[i]后,i之前的奇偶性不发生变化,i之后的奇偶性发生对调.O(N)编译即可

----------------------------
2609 最小生成树
求一棵生成树,让其边的极差最小
solution:
考虑到边一定是相邻的,排一次序后,然后枚举最小边O(M),然后求最小生成树即可.
O(M*M)


---------------------------
2542  二分+贪心
你要完成一个𝑚页的作业，手里有𝑛杯咖啡，每一杯咖啡有一个咖啡因强度值𝑎𝑖，能支撑你写𝑎𝑖页作业。每一天你会选择一些咖啡喝掉，对于当天喝的第i杯咖啡，咖啡因的强度会减弱𝑖−1单位，减到0就不再减小。问你最少经过几天能完成作业。
solution:
考虑到单调性,如果2天能完成的作业,那么3天也一定可以完成.
why?因为如果2天某一天喝了>=2杯,那么把其中一杯拿给第三天喝,那么一定不会更差,因为第三天第一杯强度不会减弱.
然后如何check是否合法呢.
从大往小,从左往右尽可能均匀分配

---------------
2616 滑动窗口模板
问长度为N的所有连续子序列的极差
solution:
滑动窗口O(N)

---------------------
2538
问能否重排列一个队列,使其后一个数是前一个的两倍或者1/3.
0,有相同的数显然不符合 
1,该图是一个DAG,如果一个点有两个后继,那么一定是n/3或者2*n,考虑到走任意一条点都不可能把另外一个点cover到
2,---> 所有点必须入度出度为1?
****************************
逆元的详细过程

--------------------------
2620
给你一个长度为N的序列,每次可以花max(a[i],a[i+1])的代价将(a[i],a[i+1])替换为
max(a[i],a[i+1]),问变成长度为1的最小代价是?
solution:
ans+=sigma max(a[i],a[i+1]) (i from 1 to N-1)
why?
因为每次选择最小的比较优秀，同样是将长度为N变成N-1,如果你选择代价大的话，消费也会变大，而且代价大的会继续保留在N-1代．而选择代价小的，消费小，虽然代价小的也会一直流传下去.
所以每次选择代价最小的．推导出的公式如上.
