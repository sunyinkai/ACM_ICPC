1007 
将一堆物品分为两组,使得它们的和的差最小
solution:
求和后,除以2,然后dp

------------------------------
1010
询问>=当前数字,且因子只含有2,3,5的数
solution:
暴力即可,注意爆long long,以及在合适的地方跳出

------------------------------
1014
x^2%P==A P,A<=1e6求x
solution:
暴力

----------------------------------
1031
铺砖问题
solution:
递推,假如当前已经铺到了n,如果竖着铺,f[n]=f[n-1],如果横着铺f[n]=f[n-2]
------------------------------------
1024
矩阵中不重复的元素
输入n,m,a,b,代表矩阵有m行n列,其中坐标(i,j)的矩阵的值为(a+i)^(b+j)
solution:
取自然对数,然后sort and count
--------------------------------------
1815 
循环最大字段,N<=5e4
---
solution 1:
1.正常的最大连续序列2.开始在尾，结束的首
第一种正常求，第二种考虑答案的组成，开始的一段+到结尾的一段，那么中间为什么去掉呢，因为中间那段和为负数，
只要求出负数最大的连续子序列去掉就行了max(ans1,sum+ans2);
---
solution 2:
将环打开,单调栈?
如果将环打开,直接dp会有问题, 59 -17 209 -10
---
solution 3:
将数组复制加倍后
求前缀和,枚举串的最后一个位置,RMQ做
-----------------------------
1062
序列中最大的数
solution:
直接模拟递推即可
-----------------------------
1094
N<=1e4;询问是否存在区间和为K
solution 1:
前缀和暴力 O(N^2)
--------
solution 2:
前缀和+hash
枚举左端点,询问右端点是否存在.
从右往左扫描,O(N)
-----------------------------
1119
询问从左上走到右下的方案数
solution:
C(n+m-2,m-1)
---------------------------
1315
给你一个集合，询问至少删除多少个数，才能让任意子集的or不等于给定的数X
solution:
考虑到or的性质，只增不减，所以只考虑,X的二进制下为0的对应位为0的哪些数。
然后取相应1位上的最小值即可.
---------------------------
1413
权势二进制，简单题
-------------------------
1428
问安排所有活动至少需要几个多少个教室
solution:
前缀和，但由于范围较大，需要用map,然后遍历
复杂度O(NlogN)
-------------------------
1432
独木舟装人问题
solution:
贪心，排序后双指针
---------------------------
1266
经典的蚂蚁问题
solution:
只需要蚂蚁碰头后，返回，相当于错过去即可
---------------------------
1278
所有圆的圆心都在x坐标上，给出圆心(0,p)和半径r,求相离圆的对数
solution:
问题等价与不相交的线段对数.
将线段排序后，然后二分.
*加深对lower_bound以及重载的理解.
sort(no,no+N);
long long ans=0;
for(int i=0;i<N-1;++i){
	line t;
	t.l=no[i].r+1;t.r=-INF;
	int id=lower_bound(no+i+1,no+N,t)-no;
	ans+=N-id;
}
------------------------------
1138
给定一个整数N问是否能分解为一段连续整数和
solution:
分解因子即可
--------------------------------
*1417 
一个简单的概率问题，但很有趣
solution:
考虑到美女应该不偏向于任何一边即可

-------------------------------
1433
给你一堆5和0,问你能够构成最大的是90的倍数的数
solution:
10进制下能%9==0的数的特点是，各数位上和是9的倍数
--------------------------------
1067
取石头游戏,每次取1,3,4颗
solution:
	简单的NP转移
	print("B" if N%7==2 or N%7==0 else "A")
---------------------------------
1133
经典的活动安排问题,问最多能安排多少活动
solution:
	越早结束越优先
---------------------------------
1596
有N个货物，第i个重量是2^w[i],每次搬运的时候重量必须是2的幂
问至少搬几次
solution:
	数组模拟进位，即可
---------------------------------
1095
一个单词a如果通过交换单词中字母的顺序可以得到另外的单词b，那么定义b是a的Anigram，例如单词army和mary互为Anigram。另：相同的2个单词不算Anigram。
solution:
	两个map,一个记录原来的数量，另外一个记录最小表示法的数量
Q:怎么求一个string的最小表示法

------------------------------------
1279  好题
扔盘子.一口高为N的水井,每层的宽度为a_i,现在往里面扔M个盘子，
宽度为b_i,高度为1层水井的高度,盘子有几种命运:1,掉到井底，2,被卡住
3落到别的盘子上面.
solution 1:
	考虑到单调性，利用前缀和(f[i]:前i项的最小值)二分。
	找到大于b[i]的最小值即可。
	O(nlogn)

-------------
2513 贪心，双指针
重排列A数组，使得a[i]>b[i]的值尽可能多。
solution:
    不难发现,其实问题本质上为找最多的a[i]>b[j]
    排序后，双指针即可

------------
2518
问有多少个子区间和为S.
solution:
    枚举左端点,二分
注意:本题不能双指针，因为数组只有元素0,1不是单调递增的

-------------------
2593
bfs,二维数组上一团点到另外一团点最近的距离．
solution:
bfs即可．
注意:bfs的写法，减少数据量的入队．保证数据只入队一次．
防止MLE

----------------------
2655
将n个不相等的木棍分为m个,问这m个最长能有多长．
solution:
考虑到单调性，直接二分最后长度.
注意:做除法的时候，除数为0


-------------------
**2601 好题**
求两个数组a,b中,最长公共子串的长度.
solution:
二分长度,因为它一定满足单调性．
假设长度值为x.
对于a数组，O(N)构造hash表,代表a有哪些字符串.
对于b数组,使用同样的规则构造hash,去check由a构造的hash表中是否存在.
注意:二分一定要考虑val==0的时候.

---------------------
2654 n个数中选出k个，让相邻两个数的最小距离最大.
solution:
二分最后这个距离的大小
int lo=0-1,hi=INF+1;

---------------------
2597 **好题**　递推
A
AB
ABBA
...
A->AB
B->BA
求第N行第K个．
solution:
找递推，找与上一行的关系



*********************************************
knowledge:
最小表示法求法
当前排列的下一个排列
二分一定要考虑val==0的时候.
