1110_C 
求最大的gcd(a,b),a+b=x
solution:
因为gcd(a,b)==gcd(a+b,b)
所以gcd(a,b)=gcd(x,b)
O(sqrt(x))求x的最大因子即可.

118_D2


1151D 贪心
给你N个a_i,b_i
对其进行排序，最后的不满意度是:
sigma (i-1)*a_i + (N-i)*b_i
solution:
假设条件,然后重载小于符号

1151E 还没做，感觉有意思

888_D:错排+组合数
从N个数，选择至少N-K个数在原位上的方案数.
N<=1000,K<=4

1140_C:贪心,思维,好题 
从N个pair(x,y)中选择最多K个,让他们的(sigmax)*min(y)最大.
solution:
不如将y从大到小排序,然后枚举,然后从已经遍历过的选择topK即可.优先队列维护.
例子:
8 6 7 1    k=2
6 4 3 15

1181A:思维
给你一棵树，你可以从中任选两个叶节点，中途路径上可以加上任意树。
问，能否让这棵树边达到任意的权值.
solution:
除叶节点外任意节点的儿子>=2,
