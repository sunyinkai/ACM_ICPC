1110_C 
求最大的gcd(a,b),a+b=x
solution:
因为gcd(a,b)==gcd(a+b,b)
所以gcd(a,b)=gcd(x,b)
O(sqrt(x))求x的最大因子即可.

118_D2


1151D 贪心
给你N个a_i,b_i
对其进行排序，最后的不满意度是:
sigma (i-1)*a_i + (N-i)*b_i
solution:
假设条件,然后重载小于符号

1151E 还没做，感觉有意思

888_D:错排+组合数
从N个数，选择至少N-K个数在原位上的方案数.
N<=1000,K<=4
